// Generated by utils/GenerateHighwayOps.swift. Do not edit.

internal import CHighway

/// The direction `Highway.sort(_:order:)` and its neighbours order keys in.
public enum SortOrder: Sendable {
    case ascending
    case descending
}

/// A key type that Highway's vectorized sort supports.
///
/// Unlike the ops, sorting picks its target at run time, so it uses the best target the
/// processor has rather than the one the compiler was told to assume.
public protocol HighwaySortable {
    static func sort(_ keys: UnsafeMutableBufferPointer<Self>, order: SortOrder)
    static func partialSort(
        _ keys: UnsafeMutableBufferPointer<Self>,
        keeping count: Int,
        order: SortOrder
    )
    static func select(_ keys: UnsafeMutableBufferPointer<Self>, at count: Int, order: SortOrder)
}

extension UInt16: HighwaySortable {
    public static func sort(
        _ keys: UnsafeMutableBufferPointer<UInt16>,
        order: SortOrder
    ) {
        guard let base = keys.baseAddress, keys.count > 1 else { return }
        switch order {
        case .ascending:
            unsafe hwy.VQSort(base, keys.count, hwy.SortAscending())
        case .descending:
            unsafe hwy.VQSort(base, keys.count, hwy.SortDescending())
        }
    }
    public static func partialSort(
        _ keys: UnsafeMutableBufferPointer<UInt16>,
        keeping count: Int,
        order: SortOrder
    ) {
        guard let base = keys.baseAddress, keys.count > 1 else { return }
        switch order {
        case .ascending:
            unsafe hwy.VQPartialSort(base, keys.count, count, hwy.SortAscending())
        case .descending:
            unsafe hwy.VQPartialSort(base, keys.count, count, hwy.SortDescending())
        }
    }
    public static func select(
        _ keys: UnsafeMutableBufferPointer<UInt16>,
        at count: Int,
        order: SortOrder
    ) {
        guard let base = keys.baseAddress, keys.count > 1 else { return }
        switch order {
        case .ascending:
            unsafe hwy.VQSelect(base, keys.count, count, hwy.SortAscending())
        case .descending:
            unsafe hwy.VQSelect(base, keys.count, count, hwy.SortDescending())
        }
    }
}

extension UInt32: HighwaySortable {
    public static func sort(
        _ keys: UnsafeMutableBufferPointer<UInt32>,
        order: SortOrder
    ) {
        guard let base = keys.baseAddress, keys.count > 1 else { return }
        switch order {
        case .ascending:
            unsafe hwy.VQSort(base, keys.count, hwy.SortAscending())
        case .descending:
            unsafe hwy.VQSort(base, keys.count, hwy.SortDescending())
        }
    }
    public static func partialSort(
        _ keys: UnsafeMutableBufferPointer<UInt32>,
        keeping count: Int,
        order: SortOrder
    ) {
        guard let base = keys.baseAddress, keys.count > 1 else { return }
        switch order {
        case .ascending:
            unsafe hwy.VQPartialSort(base, keys.count, count, hwy.SortAscending())
        case .descending:
            unsafe hwy.VQPartialSort(base, keys.count, count, hwy.SortDescending())
        }
    }
    public static func select(
        _ keys: UnsafeMutableBufferPointer<UInt32>,
        at count: Int,
        order: SortOrder
    ) {
        guard let base = keys.baseAddress, keys.count > 1 else { return }
        switch order {
        case .ascending:
            unsafe hwy.VQSelect(base, keys.count, count, hwy.SortAscending())
        case .descending:
            unsafe hwy.VQSelect(base, keys.count, count, hwy.SortDescending())
        }
    }
}

extension UInt64: HighwaySortable {
    public static func sort(
        _ keys: UnsafeMutableBufferPointer<UInt64>,
        order: SortOrder
    ) {
        guard let base = keys.baseAddress, keys.count > 1 else { return }
        switch order {
        case .ascending:
            unsafe hwy.VQSort(base, keys.count, hwy.SortAscending())
        case .descending:
            unsafe hwy.VQSort(base, keys.count, hwy.SortDescending())
        }
    }
    public static func partialSort(
        _ keys: UnsafeMutableBufferPointer<UInt64>,
        keeping count: Int,
        order: SortOrder
    ) {
        guard let base = keys.baseAddress, keys.count > 1 else { return }
        switch order {
        case .ascending:
            unsafe hwy.VQPartialSort(base, keys.count, count, hwy.SortAscending())
        case .descending:
            unsafe hwy.VQPartialSort(base, keys.count, count, hwy.SortDescending())
        }
    }
    public static func select(
        _ keys: UnsafeMutableBufferPointer<UInt64>,
        at count: Int,
        order: SortOrder
    ) {
        guard let base = keys.baseAddress, keys.count > 1 else { return }
        switch order {
        case .ascending:
            unsafe hwy.VQSelect(base, keys.count, count, hwy.SortAscending())
        case .descending:
            unsafe hwy.VQSelect(base, keys.count, count, hwy.SortDescending())
        }
    }
}

extension Int16: HighwaySortable {
    public static func sort(
        _ keys: UnsafeMutableBufferPointer<Int16>,
        order: SortOrder
    ) {
        guard let base = keys.baseAddress, keys.count > 1 else { return }
        switch order {
        case .ascending:
            unsafe hwy.VQSort(base, keys.count, hwy.SortAscending())
        case .descending:
            unsafe hwy.VQSort(base, keys.count, hwy.SortDescending())
        }
    }
    public static func partialSort(
        _ keys: UnsafeMutableBufferPointer<Int16>,
        keeping count: Int,
        order: SortOrder
    ) {
        guard let base = keys.baseAddress, keys.count > 1 else { return }
        switch order {
        case .ascending:
            unsafe hwy.VQPartialSort(base, keys.count, count, hwy.SortAscending())
        case .descending:
            unsafe hwy.VQPartialSort(base, keys.count, count, hwy.SortDescending())
        }
    }
    public static func select(
        _ keys: UnsafeMutableBufferPointer<Int16>,
        at count: Int,
        order: SortOrder
    ) {
        guard let base = keys.baseAddress, keys.count > 1 else { return }
        switch order {
        case .ascending:
            unsafe hwy.VQSelect(base, keys.count, count, hwy.SortAscending())
        case .descending:
            unsafe hwy.VQSelect(base, keys.count, count, hwy.SortDescending())
        }
    }
}

extension Int32: HighwaySortable {
    public static func sort(
        _ keys: UnsafeMutableBufferPointer<Int32>,
        order: SortOrder
    ) {
        guard let base = keys.baseAddress, keys.count > 1 else { return }
        switch order {
        case .ascending:
            unsafe hwy.VQSort(base, keys.count, hwy.SortAscending())
        case .descending:
            unsafe hwy.VQSort(base, keys.count, hwy.SortDescending())
        }
    }
    public static func partialSort(
        _ keys: UnsafeMutableBufferPointer<Int32>,
        keeping count: Int,
        order: SortOrder
    ) {
        guard let base = keys.baseAddress, keys.count > 1 else { return }
        switch order {
        case .ascending:
            unsafe hwy.VQPartialSort(base, keys.count, count, hwy.SortAscending())
        case .descending:
            unsafe hwy.VQPartialSort(base, keys.count, count, hwy.SortDescending())
        }
    }
    public static func select(
        _ keys: UnsafeMutableBufferPointer<Int32>,
        at count: Int,
        order: SortOrder
    ) {
        guard let base = keys.baseAddress, keys.count > 1 else { return }
        switch order {
        case .ascending:
            unsafe hwy.VQSelect(base, keys.count, count, hwy.SortAscending())
        case .descending:
            unsafe hwy.VQSelect(base, keys.count, count, hwy.SortDescending())
        }
    }
}

extension Int64: HighwaySortable {
    public static func sort(
        _ keys: UnsafeMutableBufferPointer<Int64>,
        order: SortOrder
    ) {
        guard let base = keys.baseAddress, keys.count > 1 else { return }
        switch order {
        case .ascending:
            unsafe hwy.VQSort(base, keys.count, hwy.SortAscending())
        case .descending:
            unsafe hwy.VQSort(base, keys.count, hwy.SortDescending())
        }
    }
    public static func partialSort(
        _ keys: UnsafeMutableBufferPointer<Int64>,
        keeping count: Int,
        order: SortOrder
    ) {
        guard let base = keys.baseAddress, keys.count > 1 else { return }
        switch order {
        case .ascending:
            unsafe hwy.VQPartialSort(base, keys.count, count, hwy.SortAscending())
        case .descending:
            unsafe hwy.VQPartialSort(base, keys.count, count, hwy.SortDescending())
        }
    }
    public static func select(
        _ keys: UnsafeMutableBufferPointer<Int64>,
        at count: Int,
        order: SortOrder
    ) {
        guard let base = keys.baseAddress, keys.count > 1 else { return }
        switch order {
        case .ascending:
            unsafe hwy.VQSelect(base, keys.count, count, hwy.SortAscending())
        case .descending:
            unsafe hwy.VQSelect(base, keys.count, count, hwy.SortDescending())
        }
    }
}

extension Float: HighwaySortable {
    public static func sort(
        _ keys: UnsafeMutableBufferPointer<Float>,
        order: SortOrder
    ) {
        guard let base = keys.baseAddress, keys.count > 1 else { return }
        switch order {
        case .ascending:
            unsafe hwy.VQSort(base, keys.count, hwy.SortAscending())
        case .descending:
            unsafe hwy.VQSort(base, keys.count, hwy.SortDescending())
        }
    }
    public static func partialSort(
        _ keys: UnsafeMutableBufferPointer<Float>,
        keeping count: Int,
        order: SortOrder
    ) {
        guard let base = keys.baseAddress, keys.count > 1 else { return }
        switch order {
        case .ascending:
            unsafe hwy.VQPartialSort(base, keys.count, count, hwy.SortAscending())
        case .descending:
            unsafe hwy.VQPartialSort(base, keys.count, count, hwy.SortDescending())
        }
    }
    public static func select(
        _ keys: UnsafeMutableBufferPointer<Float>,
        at count: Int,
        order: SortOrder
    ) {
        guard let base = keys.baseAddress, keys.count > 1 else { return }
        switch order {
        case .ascending:
            unsafe hwy.VQSelect(base, keys.count, count, hwy.SortAscending())
        case .descending:
            unsafe hwy.VQSelect(base, keys.count, count, hwy.SortDescending())
        }
    }
}

extension Double: HighwaySortable {
    public static func sort(
        _ keys: UnsafeMutableBufferPointer<Double>,
        order: SortOrder
    ) {
        guard let base = keys.baseAddress, keys.count > 1 else { return }
        switch order {
        case .ascending:
            unsafe hwy.VQSort(base, keys.count, hwy.SortAscending())
        case .descending:
            unsafe hwy.VQSort(base, keys.count, hwy.SortDescending())
        }
    }
    public static func partialSort(
        _ keys: UnsafeMutableBufferPointer<Double>,
        keeping count: Int,
        order: SortOrder
    ) {
        guard let base = keys.baseAddress, keys.count > 1 else { return }
        switch order {
        case .ascending:
            unsafe hwy.VQPartialSort(base, keys.count, count, hwy.SortAscending())
        case .descending:
            unsafe hwy.VQPartialSort(base, keys.count, count, hwy.SortDescending())
        }
    }
    public static func select(
        _ keys: UnsafeMutableBufferPointer<Double>,
        at count: Int,
        order: SortOrder
    ) {
        guard let base = keys.baseAddress, keys.count > 1 else { return }
        switch order {
        case .ascending:
            unsafe hwy.VQSelect(base, keys.count, count, hwy.SortAscending())
        case .descending:
            unsafe hwy.VQSelect(base, keys.count, count, hwy.SortDescending())
        }
    }
}

extension Highway {
    /// Sorts `keys` in place with Highway's vectorized quicksort.
    @export(implementation)
    public static func sort<Key: HighwaySortable>(
        _ keys: inout [Key],
        order: SortOrder = .ascending
    ) {
        keys.withUnsafeMutableBufferPointer { unsafe Key.sort($0, order: order) }
    }

    /// Orders `keys` so that its first `count` elements are the ones a full sort would
    /// put there, in the order a full sort would put them in.
    @export(implementation)
    public static func partialSort<Key: HighwaySortable>(
        _ keys: inout [Key],
        keeping count: Int,
        order: SortOrder = .ascending
    ) {
        keys.withUnsafeMutableBufferPointer {
            unsafe Key.partialSort($0, keeping: count, order: order)
        }
    }

    /// Orders `keys` so that the element at `index` is the one a full sort would put
    /// there, and no element before it compares after it.
    @export(implementation)
    public static func select<Key: HighwaySortable>(
        _ keys: inout [Key],
        at index: Int,
        order: SortOrder = .ascending
    ) {
        keys.withUnsafeMutableBufferPointer { unsafe Key.select($0, at: index, order: order) }
    }
}
